<div class="mb-4 d-flex justify-content-center align-items-center">
  <% if logged_in? %>
    <%= form_with url: main_history_path, method: :get, local: true, html: { class: "d-flex align-items-center" } do |f| %>
      <%= f.text_field :q, placeholder: "検索ワード", class: "form-control me-2", style: "width: 250px;" %>
      <%= f.submit "検索", class: "btn btn-outline-dark" %>
    <% end %>
  <% else %>
    <p>ログインしてください。</p>
  <% end %>

  <div>
  </div>
</div>
